Напиши полноценную программу для микроконтроллера **ESP8266** (например, NodeMCU v3) в среде **Platformio**.

### 📌 Функциональные требования

Реализовать проект управления светодиодной лентой **WS2812 (NeoPixel)** через веб-интерфейс.

Микроконтроллер должен:

1. Подключаться к Wi-Fi сети в режиме STA (подключение к существующему роутеру).
2. Запускать HTTP-сервер на порту 80.
3. Отдавать HTML-страницу с интерфейсом управления.
4. Позволять изменять:

   * Яркость (0–255)
   * Теплоту света (цветовая температура от тёплого до холодного белого)
5. Обновлять ленту в реальном времени без перезагрузки страницы (через AJAX или fetch API).
6. Сохранять последние выбранные параметры в EEPROM и восстанавливать их при перезапуске.

---

### ⚙️ Аппаратная конфигурация

* Микроконтроллер: ESP8266
* Лента: WS2812
* Количество светодиодов: 30
* Пин подключения ленты: D4 (GPIO2)
* Библиотека для ленты: `Adafruit_NeoPixel`
* Web-сервер: `ESP8266WebServer`

---

### 🎛 Требования к управлению цветом

1. Управление теплотой реализовать через плавный переход:

   * 2700K (тёплый белый, больше красного)
   * 6500K (холодный белый, больше синего)

2. Реализовать преобразование "температура → RGB".

3. Яркость должна применяться после расчёта цвета.

---

### 🌐 Требования к веб-интерфейсу

HTML-страница должна содержать:

* Ползунок яркости
* Ползунок теплоты
* Кнопку вкл/выкл
* Отображение текущих значений
* Минималистичный современный стиль (CSS внутри страницы)
* Обновление параметров без перезагрузки страницы

Использовать:

* fetch API для отправки GET или POST запросов
* JSON-ответ от ESP8266

---

### 🧠 Архитектурные требования

* Код должен быть структурирован:

  * Функция подключения к WiFi
  * Функция инициализации ленты
  * Функция расчёта RGB из температуры
  * Функция применения яркости
  * Обработчики HTTP-запросов
* Добавить вывод отладочной информации в Serial Monitor.
* Обработать ситуацию отсутствия WiFi (автопереподключение).

---

### 💾 Дополнительные требования

* Использовать EEPROM для хранения:

  * Яркости
  * Температуры
* Все необходимые библиотеки указать в начале кода.

---

### 📦 Формат ответа

1. Полный исходный код `.ino`
2. Краткое объяснение архитектуры
3. Схема подключения
4. Список используемых библиотек
