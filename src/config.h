// config.h - Конфигурация проекта
#pragma once

// ========== ПАРАМЕТРЫ ПОДКЛЮЧЕНИЯ ==========
#define LED_PIN           D4      // Пин подключения ленты
#define NUM_LEDS          30      // Количество светодиодов в ленте

// ========== ОГРАНИЧЕНИЯ ПО ТОКУ ==========
#define MAX_CURRENT_MA     2000    // Максимальный ток блока питания (мА)
#define CURRENT_PER_LED_MA 60     // Ток на один светодиод при белом 100% (мА)

// ========== ДИАПАЗОНЫ ПАРАМЕТРОВ ==========
#define MIN_BRIGHTNESS    0       // Минимальная яркость (%)
#define MAX_BRIGHTNESS    100     // Максимальная яркость (%)
#define MIN_TEMPERATURE   1000    // Минимальная температура (K)
#define MAX_TEMPERATURE   4000    // Максимальная температура (K)

// ========== ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ ==========
#define DEFAULT_BRIGHTNESS    75      // Яркость по умолчанию (%)
#define DEFAULT_TEMPERATURE   3000    // Температура по умолчанию (K)
#define DEFAULT_POWER_ON      true    // Питание включено по умолчанию

// ========== ПАРАМЕТРЫ ВЕБ-ИНТЕРФЕЙСА ==========
#define HTML_TITLE         "WS2812 Контроллер"  // Заголовок страницы